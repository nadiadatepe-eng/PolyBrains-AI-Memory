# CP-C3 — v0.3 release gate

**Run:** 2026-08-28

Every CP-C0–C2 execution is preserved in paired human-readable and machine-readable results where
applicable: CP-C1 development, CP-C1 held-out, and CP-C2 replication. CP-C0 is a preregistration,
not an executed result. No mixed, negative, or invalid CP-C run occurred.

The complete gate passed from a clean exported checkout: all 9 unit tests, the frozen v0.1
replication, v0.2 held-out and three-seed retrieval checks, and v0.3 development and replication
checks. `git diff --check` also passed.

The narrow result is that explicit active evidence links selected the supported outcome and
preserved abstention on these frozen contradiction fixtures. It does not establish that
provenance, linked evidence, source labels, similarity, confidence, agreement, or recency are
inherently correct. The retained scorer remains a benchmark policy outside the core API and adds
no dependency.

Evidence: `reports/cp-c1-development.md`, `reports/cp-c1-development-result.json`,
`reports/cp-c1-heldout.md`, `reports/cp-c1-heldout-result.json`,
`reports/cp-c2-replication.md`, and `reports/cp-c2-result.json`.
